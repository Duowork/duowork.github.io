---

---

<section
  id="hero-section"
  class="h-[40rem] md:h-[50rem] lg:h-[100vh] bg-no-repeat bg-top bg-cover relative"
>
  <div
    id="overlay"
    class="!h-full absolute size-full top-0 left-0 bg-[#000000cc]"
  >
    <div
      id="hero-container"
      class="flex flex-col item-center justify-center h-full"
    >
      <h1
        class="w-full max-w-[63rem] text-2xl md:text-4xl lg:text-6xl text-white text-center font-bold self-center mb-3 md:mb-5 leading-tight"
      >
        Custom Software Solutions and Product Strategy For Bold Businesses.
      </h1>

      <p
        id="hero-description"
        class="max-w-[35rem] text-white text-center mt-10 self-center text-lg px-[2.25rem] sm:px-0 font-light sm:w-1/2"
      >
        We help businesses automate and scale their business vision to the top.
      </p>

      <a
        href="/contact"
        id="hero-button"
        class="flex items-center justify-center mt-10 self-center text-white rounded-xl cta-btn text-center max-w-[12.5rem] w-[12.5rem] !bg-transparent border !border-duo-green-200"
        title="Get started"
        type="button"
      >
        Talk to us
      </a>
    </div>
  </div>
</section>

<style lang="scss">
  @import "../../styles/main.scss";

  section {
    background-image: url("team.jpeg");

    // Opaque black overlay
    #overlay {
      #hero-container {
        #hero-button {
          // width: 200px;
          // max-width: 200px;
          // border: 1px solid $accent-color;
          // background-color: transparent !important;

          #button-text {
            display: inline-block;
            width: 70%;
            font-weight: 300;
            font-size: medium;
          }
        }
      }
    }
  }
</style>
